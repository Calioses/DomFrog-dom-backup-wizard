#TODO Add service to launch/close in lockstep with dominions6.exe

Watcher windows service loop. If dominions6.exe exist in tasklist. launch cmd domfrog --daemon. Doesn't exist. Close domfog.exe if exists

#TODO add multi-nation per game support

Split the save folders into per nation sub folders. 
Create hash individual to nation.
Use game-name+nation-name as uuid

#TODO Make an option flag to overwrite instead of new folder 

#TODO Clean up UI Decision tree 

#TODO? maybe make it support linux and mac

#TODO add an install/uninstall option to the launch UI
Delete folder in appdata. Delete service? "sc delete domfrog" and "Remove-Item -Path "$env:APPDATA\domfrog" -Recurse -Force" probably enough